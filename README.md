## Bookmark Saver

## Local Storage Practice with an array of objects

## Had issues with:


    if (!isValidUrl(url)) {
        setStatus("Invalid URL");
        return;
    }  

    and

    const bookmarks = loadBookmarks();